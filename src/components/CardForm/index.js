import React from 'react';

import './index.css';

export const CardForm = (props) => (
	<div className='card-form' {...props} />
);

export const CardFormFooter = (props) => (
	<div className='card-form__footer' {...props} />
)